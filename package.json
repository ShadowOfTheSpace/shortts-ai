{
  "author": "ShadowOfTheSpace",
  "name": "shortts-ai",
  "scripts": {
    "build:web-app": "npm run build -w web-app",
    "build:server": "npm run build -w server",
    "build:shared": "npm run build -w shared",
    "build": "npm run build:shared && npm run build:server && npm run build:web-app",
    "start:dev:web-app": "npm run start:dev -w web-app",
    "start:dev:server": "npm run start:dev -w server"
  },
  "workspaces": [
    "server",
    "shared",
    "web-app"
  ],
  "devDependencies": {
    "tsc-alias": "1.8.10",
    "typescript": "5.5.2"
  }
}
