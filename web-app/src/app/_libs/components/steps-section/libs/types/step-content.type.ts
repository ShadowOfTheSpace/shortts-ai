import { IconName } from "~/_libs/types/types";

type StepContent = {
  backgroundImageUrl: string;
  description: string;
  iconName: IconName;
  imageUrl: string;
  title: string;
};

export { type StepContent };
